//no imports? because we are exporting already



























//LOGIN don't do yet!
//just creating functions right now, so params just need to make sense
//but it really matters in your scripts.js file

// export function createLogin(userNameInput,passWordInput) {
//     const information = {
//         username: userNameInput,
//         password: passwordInput
//     }
//     return information
// }

// login(username, password):
//     # Check if the username and password are valid (you would typically query a database here)
//     if username == "example_user" and password == "password123":
//         # Authentication successful
//         return True
//     else:
//         # Authentication failed
//         return False

// export function screenLogin(userName,passWord) {
//     if (username === userName || password === passWord) {  
//         return `Welcome ${userName}`
//     }
//     if (username === "" || password === "") {
//         return 'Please enter both your username and password.'
//     } else {
        
//     }
// }

//button.disabled()